---
page_title: "Provider: NPS"
description: |-
  The NPS provider allows managing a Workshop instance.
---

# NPS Provider

The NPS provider allows managing a North Pole Security Workshop instance using
Terraform.

Use the navigation to the left to read about the available resources. If there's
a resource type missing that you would like to manage with Terraform, please let
us know.

## Example Usage

### With short-lived access tokens

```terraform
provider "nps" {
  endpoint = "api.tenant.workshop.cloud"
}
```

If no API key is provided, the provider will default to using short-lived access
tokens.

The provider itself is capable of creating these tokens by passing the
`-login` flag to the provider binary. If you attempt to use the provider without
an API key and with no token available, it will print an error string including
the full command to run to login. Running this command will print a URL and open
a browser to a login page for your Workshop instance and ask you to confirm the
device code. Once confirmed the provider will retrieve the token and store it
for future Terraform invocations. You will need to repeat this process if the
token expires.

The generated token will have the same permissions as the user that logs in.

### With API keys

```terraform
provider "nps" {
  endpoint = "api.tenant.workshop.cloud"
  api_key  = "npsws_sk_aabbbbcccdddeee"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) The API key to use. Can also be supplied using the `WORKSHOP_API_KEY` environment variable. If no API key is provided, the provider will attempt to use a stored short-lived user token.
- `endpoint` (String) The base URL for the Workshop instance. Can also be supplied using the `WORKSHOP_ENDPOINT` environment variable. `NPS_ENDPOINT` remains available as a deprecated fallback.
